const config = {
  // 1. LOCALHOST (Uncomment this for Local Development)
  API_BASE_URL: "http://localhost:5000/api", 

  // 2. LIVE SERVER (Comment this out)
  // API_BASE_URL: "https://skillbridge-backend.onrender.com/api", 
};

export default config;
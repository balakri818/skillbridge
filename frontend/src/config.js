const config = {
  // 1. LOCALHOST (Use this while coding on your computer)
  // API_BASE_URL: "http://localhost:5000/api",

  // 2. LIVE SERVER (Uncomment this BEFORE pushing to GitHub for deployment)
  //YZ NOTE: You will replace this URL with your actual Render URL later
  API_BASE_URL: "https://skillbridge-backend.onrender.com/api", 
};

export default config;